package Main;

import View.ViewHandler;
import javafx.application.Application;

public class Main {
    // ne pas se préoccuper de ce code, il sera toujours le même en javafx
    public static void main(String[] args) {
        Application.launch(ViewHandler.class);
    }
}
